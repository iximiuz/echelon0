use std::fs::File;


pub struct Reader {
}

impl Reader {
    pub fn new() -> Reader {
        Reader {}
    }

    pub fn read_chunk(to: &mut String) -> Result<(), E> {

        // let f = File::open("input.txt").expect("File not found");

    }
}
